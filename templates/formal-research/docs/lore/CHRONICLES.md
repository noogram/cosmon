# Chronicles

Append-only narrative memory. Each entry illuminates a principle — not a
changelog, not a decision log. The raw material from which future talks,
READMEs, and papers will be drawn.

Two observation patterns feed this file:
- **Design B (blob chronicler)**: `just mycelium` — one worker reads all
  artifacts, appends 0-3 entries. Formula: `mycelium.formula.toml`.
- **Design A (phantom DAG)**: `just physarum` — a planner nucleates N
  sequential hyphae, each with fresh context, reading accumulated CHRONICLES
  plus its host molecule's artifacts. Formula: `physarum.formula.toml`.

---
