[package]
name = "cosmon-surface-canon"
publish.workspace = true
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
# Frontier crate (ADR-092): consumed at build time by frontier crates
# (cosmon-thin-cli) — keep on Apache-2.0 to preserve permissive boundary.
license = "AGPL-3.0-only"
description = "Parser for the enriched §8p surface event log (data/surface_events.txt) — the single canonical declaration of the cosmon RPP API surface."

[dependencies]

[lints]
workspace = true
