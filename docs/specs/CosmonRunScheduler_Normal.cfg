\* Model 1 — Normal scheduler operation.
\* S3 is enabled (the guard that `patrol-propel` only fires while
\* sediment = 0). Autopilot guard also enabled as a stricter overlay
\* (ADR-048 backlog sanity with threshold 2).
\* Expected: every safety invariant holds; L2 liveness holds.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTRAINT StateBound
CONSTANTS
    MaxTime          = 3
    MaxSediment      = 1
    S3Enabled        = TRUE
    AutopilotEnabled = TRUE
    BacklogThreshold = 2
INVARIANTS
    TypeOK
    S1_NonOverlap
    S2_WindowIsClosed
    S3_PurgeBeforeRespawn
PROPERTIES
    L2_EventualFinish
