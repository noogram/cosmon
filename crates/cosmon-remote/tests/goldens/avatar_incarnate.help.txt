`POST /v1/avatar/{instance_id}/incarnate` — bind moule→avatar

Usage: cosmon-remote avatar incarnate [OPTIONS] --pilote <PILOTE> --tenant <TENANT> --juridiction <JURIDICTION> <INSTANCE_ID>

Arguments:
  <INSTANCE_ID>  

Options:
      --pilote <PILOTE>            Pilote DID (e.g. `did:key:z6Mk...`)
      --profile <PROFILE>          Profile name (default: `default_profile` from config.toml, or `$COSMON_REMOTE_PROFILE`)
      --json                       Render machine-readable JSON instead of human text where applicable
      --tenant <TENANT>            Tenant identifier
      --juridiction <JURIDICTION>  ISO 3166-1 alpha-2 jurisdiction (e.g. `FR`)
      --token <TOKEN>              Bearer JWT (overrides `$COSMON_REMOTE_TOKEN`). When unset and an authenticated call is made, the CLI mints a token via the profile's `oidc_url`
  -h, --help                       Print help
