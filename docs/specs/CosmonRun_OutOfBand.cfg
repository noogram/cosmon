\* Model 2 — Out-of-band BypassMerge enabled, async crashes disabled.
CHECK_DEADLOCK FALSE
\* Expected: I3, I4, I6, I7 still hold (they don't touch branch_merged).
\* I9 must FAIL — TLC produces a counterexample where branch_merged
\* becomes TRUE while mol_status is Pending or Running.
\* This is the mechanical proof of ADR-052 §"Out-of-band classification":
\* I9 is the Gödel sentence — true when the environment is closed,
\* unenforceable from inside the spec when the environment can write.
SPECIFICATION Spec
CONSTANTS
    Mol = {m1, m2, m3}
    MaxSeqno = 2
    AsyncCrashesEnabled = FALSE
    OutOfBandEnabled    = TRUE
    T_STALL  = 99
    MaxClock = 1
INVARIANTS
    TypeOK
    I9_BranchMergedOnlyIfCompleted
