name: CI

# All third-party Actions pinned by 40-char commit SHA (delib-20260425-39c1).
# Tag-mutable references (@v4, @stable, @v2) are FORBIDDEN — re-pin via
# `gh api repos/<owner>/<repo>/git/refs/tags/<tag>` before bumping.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 4 * * *'

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  sovereignty:
    name: Sovereignty gate (avatar-tenant-demo bundle)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      # Post-merge backstop over the customer-traveling dist/avatar-tenant-demo/
      # bundle (also wired into build.sh + handoff.sh, on the same git-tracked
      # byte-set). Three mechanisms (spec: scripts/sovereignty-spec.md, authored
      # by the rounds 3-5 review panel):
      #  1. DENY-class (hard, fails CI) — shaped classes + known-identity
      #     literals: molecule ids, branch/worktree names, tailnet hosts,
      #     operator paths, emails, dates, IPv4, the known operator/fund/galaxy
      #     names.
      #  2. RESOLVABILITY (hard, fails CI) — the generative rule that replaced
      #     the old per-shape citation classes (ADR/§ → source paths → bead
      #     codes, each chased one round late): every path-like/citation-like
      #     reference MUST resolve to a bundle file, a public URL, or a public
      #     standard; a dangling pointer into cosmon's private corpus is denied
      #     the moment it appears, whatever its next lexical shape.
      #  3. WARN (advisory) — unknown vocabulary absent from
      #     scripts/sovereignty-allowlist.txt, the net for the NEXT proper noun.
      - run: ./scripts/sovereignty-gate.sh

  confidentiality:
    name: Confidentiality banlist (publishable surface)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      # Operator/fund identity tripwire ([B1′: R5], B1′ doc-modernization plan
      # §6-P0). Hard gate over the PUBLISHABLE surface only — the rendered mdBook
      # source (docs/book/src/), README, LICENSE/NOTICE, i.e. exactly the byte-set
      # that becomes the public docs.noogram.org site + the public repo front matter.
      # Clean today; a RE-LEAK of an operator/fund term onto that surface fails CI.
      # The whole-repo audit (`--whole-repo`) covering the INTERNAL corpus (ADRs,
      # lore) is intentionally NOT wired here: it is the pre-public-flip gate the
      # operator runs before the scrub (§6-P5), and would false-red the private
      # repo's own legitimate internal history. Promote it to a hard gate only
      # AFTER that scrub lands. Single source of the banlist: the script itself.
      - run: ./scripts/confidentiality-banlist.sh
      - run: ./scripts/confidentiality-banlist.test.sh

  docs-one-gate:
    name: Docs one-gate (tool surface = stranger-installable)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      # The "one gate" docs-lint (ADR-149 §4 / ADR-126, delib-20260711-8d00 C4).
      # A public doc surface for a tool — a nav title, a section heading, or a
      # /<tool>/install.sh endpoint — exists IFF a stranger can install it
      # (public repo + release artifact). One rule, two constraints: it fails on
      # the confidential neurion PRODUCT surfacing as a section/nav/endpoint
      # (anti-leak) AND on a premature tool acquiring an install endpoint before
      # it ships (anti-vaporware). Complementary to the confidentiality banlist
      # above (which bans operator/fund IDENTITY terms). Hard on every PR; pure
      # bash + git grep over docs/book/src. Single source of the tool
      # classification: the script itself.
      - run: ./scripts/check-docs-one-gate.sh
      - run: ./scripts/check-docs-one-gate.test.sh
      # Local targets and heading fragments are a hard, offline review gate.
      # Remote probes only warn: external availability is not reproducible CI
      # state, but a dead URL or a specific path redirected to a site home is
      # still surfaced for the reviewer.
      - run: ./scripts/check-book-links.sh
      - run: ./scripts/check-book-links.test.sh

  publish-membrane:
    name: Publish membrane (runtime state · credentials · machine paths · binaries)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          # The gate audits the TRACKED tree, and check C reads symlink blobs —
          # both need real git history, not a synthesised single-commit tree.
          fetch-depth: 0
      # `CLAUDE.md` has ordered `scripts/publish.sh --check` for release-bound
      # changes since the contributor guide existed; the file never did, so the
      # sentence guarding the public projection named a property nobody
      # measured. It now exists and runs on every PR — because the two clauses
      # it owns (credentials, machine paths) are exactly the ones the other
      # referees cannot gate here: release-checklist.sh GATE 1 needs gitleaks
      # installed and GATE 4 needs the operator's private denylist, and a bare
      # CI checkout has neither, so both honestly PEND. This one needs nothing
      # but git and python3 and is therefore hard.
      - run: ./scripts/publish.sh --check
      # A green gate proves the scanner ran, not that it can red. The test
      # constructs a real violating repository per rule and fails unless the
      # gate exits non-zero AND names the offending path.
      - run: ./scripts/publish.test.sh
      # The provenance guard the nonroot-pilot bench stamps its images with
      # shipped unfalsified: removing the post-build re-check reddened nothing.
      # This constructs both ways a tree can move mid-build and asserts the
      # bench still calls the guard AFTER the build, not only before it.
      - run: ./scripts/source-provenance.test.sh
      # `just install` runs unattended after every harvest via
      # `[hooks].post_merge`, so the hook installer must never replace a
      # developer's local hook — it installs a missing one, accepts an
      # identical one, and refuses a differing one. The refusal is the whole
      # point and a refusal nothing exercises cannot fire: this test was
      # confirmed to go red (3 of 8, including "IT WAS OVERWRITTEN") against
      # the announce-then-overwrite version it replaced.
      - run: ./scripts/install-hooks.test.sh
      # The crossing no longer sits on the daily path — merges land on the
      # trunk directly since linear history was dropped — but the script is
      # kept for the exceptional scrubbed projection, and a kept tool with no
      # test is the rot this file exists to prevent. Its value is entirely in
      # what it REFUSES: a dirty tree (which would ship the previous commit's
      # content and exit green), two trunks that disagree, a waiver the
      # candidate introduces, a red publish gate, and a run whose audited
      # worktree is not the one it crosses from. Hermetic: temp-dir fixture
      # repos with a local bare remote, no network, no key — so keeping it
      # costs seconds, and dropping it would leave 1190 lines unwatched.
      - run: ./scripts/release/crossing.test.sh

  fixture-independence:
    name: Fixture independence (tautological-fixture tripwire)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      # Tautological-fixture tripwire (delib-20260710-95a7 §Q6, DoD gate a). A
      # fixture that re-derives its expected value from size_of/align_of/offset_of
      # of the type under test is a tautology — a layout bug flows into both sides
      # of the assert, so reverting the bug cannot redden the test. Fast, pure
      # bash+git grep over the tracked test surface; hard on every PR (reuses the
      # B2 command-gate mechanism, no cosmon-core change). Single source of the
      # rule + opt-out marker: the script itself.
      - run: ./scripts/check-fixture-independence.sh
      - run: ./scripts/check-fixture-independence.test.sh

  mutation-falsifier:
    name: Mutation falsifier (nightly, advisory)
    # DoD gate b — the "reverting the fix must redden the test" gate
    # (delib-20260710-95a7 §Q6). Mutation testing is SLOW (ADR-010:177): it
    # rebuilds the workspace once per mutant. It therefore runs ONLY on the
    # nightly cron, NEVER on the PR fast lane, and NEVER as a hard-abort
    # (feynman) — `continue-on-error` makes a surviving mutant a reported test
    # hole, not a red merge gate. Scoped to cosmon-core (the 90%-coverage,
    # invariant-heavy crate) to stay bounded; widen via --package / --whole-tree
    # as CI capacity allows.
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Install cargo-mutants
        run: cargo install cargo-mutants --locked
      - name: Falsifier preflight (cargo-mutants available)
        run: ./scripts/mutation-falsifier.sh --check
      - name: Mutate cosmon-core (advisory — surviving mutants are reported holes)
        continue-on-error: true
        run: ./scripts/mutation-falsifier.sh --whole-tree --package cosmon-core --timeout 180

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - run: cargo clippy --workspace --locked -- -D warnings

  # TEMPORARY, like its sibling above, and for the same investigation.
  #
  # nextest turned out to be too slow to be the instrument here: on the runner it
  # reached 503 of 7129 tests in 28 minutes, which extrapolates past six hours.
  # One process per test is the wrong shape for a suite whose tests spawn `cs`,
  # tmux and git — the process cost dominates. So this shards the AUTHORITATIVE
  # runner instead, keeping cargo/libtest's own execution model intact and only
  # reducing the surface each job covers.
  #
  # Keeping the model is the point. The hang appears under `cargo test`, and a
  # diagnostic that changes the execution model can only find a different bug.
  # Each shard therefore runs plain `cargo test` with libtest's own threading,
  # bounded, and says which crates it covered.
  #
  # Reading it, in one cycle instead of several:
  #   - one shard bounds out            -> the zone is those crates
  #   - the doctest job bounds out      -> the category is doctests, invisible to nextest
  #   - every shard passes              -> interaction or leakage BETWEEN crates
  #   - several shards bound out        -> shared pollution, not one bad test
  #
  # Gates nothing, deleted once the culprit is named.
  test-shard:
    name: Test (shard ${{ matrix.name }} — diagnostic, gates nothing)
    runs-on: ubuntu-latest
    # 80 = 30 (compile) + 25 (run) + setup, prebuild and upload. The step bounds
    # must stay strictly inside this one: a job cut on its OWN bound skips
    # `if: always()`, which is exactly how the three previous diagnostic runs
    # lost their artifacts. 60 left no room for the tail.
    timeout-minutes: 80
    continue-on-error: true
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: api
            needs_cs: true
            args: "-p cosmon-api"
            # 550s local, 84 tests
          - name: remote
            needs_cs: false
            args: "-p cosmon-remote"
            # 539s local, 231 tests
          - name: core
            needs_cs: false
            args: "-p cosmon-core -p cosmon-fuzz -p cosmon-hash -p cosmon-style"
            # 471s local
          - name: runtime
            needs_cs: true
            args: "-p cosmon-runtime -p cosmon-pilot -p cosmon-daemon -p cosmon-mcp -p cosmon-graph -p cosmon-archive -p cosmon-observability -p cosmon-process-witness -p cosmon-cockpit-http -p claudion -p cosmon -p xtask"
            # 466s local
          - name: provider
            needs_cs: false
            args: "-p cosmon-provider -p cosmon-state -p cosmon-agent-harness -p cosmon-ask -p cosmon-notary -p cosmon-ops-tools -p cosmon-registry -p cosmon-surface-canon"
            # 471s local
          - name: transport
            needs_cs: true
            args: "-p cosmon-transport -p cosmon-daemon-supervisor -p cosmon-thin-cli -p apps-transport-http -p cosmon-bridge-claude -p cosmon-scenario -p cosmon-scheduler -p cosmon-verify -p neurion-core"
            # 468s local
          - name: filestore
            needs_cs: false
            args: "-p cosmon-filestore -p cosmon-rpp-adapter -p cosmon-surface -p cosmon-client -p cosmon-cockpit -p cosmon-crashtest -p cosmon-oidc-testkit -p cosmon-thin-macro -p topon-core"
            # 477s local
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      # One cache for the whole matrix: without a shared key every shard gets its
      # own and compiles the workspace from cold, which is what made the previous
      # diagnostic job's timing unreadable.
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
        with:
          shared-key: test-shard
      # Only where a test actually shells out to the binary. `cosmon-api`,
      # `cosmon-runtime` and `cosmon-thin-cli` look for `target/debug/cs` on
      # disk and build it themselves if it is missing — which under any parallel
      # runner means several concurrent `cargo build` queued on the Cargo lock.
      # `cosmon-cli` needs no prebuild: it OWNS the binary, so cargo hands it
      # `CARGO_BIN_EXE_cs` for free. Building it in all eight shards would
      # recompile most of the graph on every runner for nothing.
      - name: Build cs binary (only for shards whose tests shell out to it)
        if: matrix.needs_cs
        run: cargo build --bin cs -p cosmon-cli --locked

      # Compile separately and bounded, so a slow build cannot present itself as
      # a hanging test — the mistake the first diagnostic job made.
      - name: Compile this shard's tests
        timeout-minutes: 30
        run: |
          set -o pipefail
          # The runner declares its own disk, before and after. Three diagnostic
          # runs have now died mid-step with no log, no artifact and no step
          # conclusion — the shape of a runner lost, not of a bound firing. A
          # full `target/debug` for this workspace is tens of gigabytes locally
          # against ~14 GB free here, so exhaustion is a candidate; these two
          # lines are what turn that from a guess into a reading.
          { echo "--- df before ---"; df -h /; } | tee shard-build.log
          cargo test --locked ${{ matrix.args }} --no-run 2>&1 \
            | tee -a shard-build.log
          { echo "--- df after ---"; df -h /; du -sh target 2>/dev/null; } \
            | tee -a shard-build.log

      - name: Run this shard
        timeout-minutes: 25
        continue-on-error: true
        run: |
          set -o pipefail
          {
            echo "shard=${{ matrix.name }}"
            echo "args=${{ matrix.args }}"
            echo "nproc=$(nproc)"
            echo "online=$(getconf _NPROCESSORS_ONLN)"
            echo "--- df ---"; df -h /
            echo "--- mem ---"; free -m || true
          } | tee shard.log

          # `set +e` + PIPESTATUS: with pipefail and the shell's implicit -e a
          # bounded-out cargo aborts the script before `rc` is written, and the
          # code is the reading. `--kill-after` covers a cargo ignoring SIGINT.
          set +e
          timeout --signal=INT --kill-after=30s 20m \
            cargo test --locked ${{ matrix.args }} \
              -- --nocapture 2>&1 | tee -a shard.log
          rc=${PIPESTATUS[0]}
          set -e

          echo "shard_rc=$rc" | tee -a shard.log
          exit "$rc"

      - name: Upload this shard's output
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: shard-${{ matrix.name }}
          path: |
            shard-build.log
            shard.log
          if-no-files-found: warn

  # The zone is `cosmon-cli`: every other crate and the doctests finish in one to
  # three minutes, and it alone does not. This job names the target inside it.
  #
  # # Why a matrix and not a loop
  #
  # The previous attempt ran the 68 targets sequentially in ONE runner, and lost
  # everything: the step began at 18:10:09, never concluded, and the job died at
  # 47 minutes without reaching its 40-minute step bound or its 90-minute job
  # bound. Five diagnostic runs now share that signature — a step that never
  # concludes and a job that dies — so the runner is being LOST rather than cut,
  # and no `if: always()` upload survives that. Four artifacts were lost to it.
  #
  # A matrix removes the dependency on an artifact entirely: **the job NAME is
  # the evidence.** One red square names its own target, read off the checks
  # list, with nothing to download. It also removes the coupling a sequential
  # loop reintroduces — a target that pollutes or exhausts its runner cannot
  # reach the ones after it, which in one runner it silently could.
  #
  # `--lib` and `--bin cs` are entries too. They pass locally at the runner's
  # four threads (142 tests in 1.2 s, 1622 in 29 s) but that is macOS, and a
  # local green cannot clear a Linux red. Here they are settled on the box.
  #
  # Deleted with the rest of this diagnostic once the culprit is fixed.
  test-cli-target:
    name: "cli/${{ matrix.name }}"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    continue-on-error: true
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: lib
            args: "--lib"
          - name: bin-cs
            args: "--bin cs"
          - name: api-cli-coverage
            args: "--test api_cli_coverage"
          - name: archive-read
            args: "--test archive_read"
          - name: archive-verify-cli
            args: "--test archive_verify_cli"
          - name: archive-wiring
            args: "--test archive_wiring"
          - name: bootstrap-monotonicity
            args: "--test bootstrap_monotonicity"
          - name: briefing-backstop-survival
            args: "--test briefing_backstop_survival"
          - name: cli
            args: "--test cli"
          - name: committee-roster-reconcile-lint
            args: "--test committee_roster_reconcile_lint"
          - name: committee-seat-dispatch
            args: "--test committee_seat_dispatch"
          - name: config-adapter-provider-override
            args: "--test config_adapter_provider_override"
          - name: config-adapters-list
            args: "--test config_adapters_list"
          - name: consent-non-blocking
            args: "--test consent_non_blocking"
          - name: constellation
            args: "--test constellation"
          - name: cross-galaxy
            args: "--test cross_galaxy"
          - name: demo
            args: "--test demo"
          - name: demo-clean-machine
            args: "--test demo_clean_machine"
          - name: demo-local-acceptance
            args: "--test demo_local_acceptance"
          - name: doctor
            args: "--test doctor"
          - name: done-attribution-stamp
            args: "--test done_attribution_stamp"
          - name: done-merge-conflict
            args: "--test done_merge_conflict"
          - name: done-molecule-base-branch
            args: "--test done_molecule_base_branch"
          - name: done-no-branch-archive
            args: "--test done_no_branch_archive"
          - name: done-pre-done-gate
            args: "--test done_pre_done_gate"
          - name: energy-budget
            args: "--test energy_budget"
          - name: ensemble-snapshot
            args: "--test ensemble_snapshot"
          - name: fleet-resolve
            args: "--test fleet_resolve"
          - name: formula-projection-parity
            args: "--test formula_projection_parity"
          - name: help-goldens
            args: "--test help_goldens"
          - name: invariant-archived-terminal
            args: "--test invariant_archived_terminal"
          - name: journal-projection
            args: "--test journal_projection"
          - name: local-adapter-output-honesty
            args: "--test local_adapter_output_honesty"
          - name: local-model-selection
            args: "--test local_model_selection"
          - name: mindguard-surface-visual
            args: "--test mindguard_surface_visual"
          - name: opt-in-share
            args: "--test opt_in_share"
          - name: pane-died-hook
            args: "--test pane_died_hook"
          - name: peek-snapshot
            args: "--test peek_snapshot"
          - name: pilot-experimental-gate
            args: "--test pilot_experimental_gate"
          - name: provider-bias-reconcile-lint
            args: "--test provider_bias_reconcile_lint"
          - name: public-attribution-domain
            args: "--test public_attribution_domain"
          - name: purge-stale-tmux
            args: "--test purge_stale_tmux"
          - name: readme-cli-table
            args: "--test readme_cli_table"
          - name: realized-model-matrix
            args: "--test realized_model_matrix"
          - name: realized-watch-reexec
            args: "--test realized_watch_reexec"
          - name: reconcile-idempotent-cli
            args: "--test reconcile_idempotent_cli"
          - name: refused-root-dispatch-leaves-no-residue
            args: "--test refused_root_dispatch_leaves_no_residue"
          - name: restart-fidelity-no-neurion
            args: "--test restart_fidelity_no_neurion"
          - name: seat-verdict-polarity-reconcile-lint
            args: "--test seat_verdict_polarity_reconcile_lint"
          - name: sensorium-strip
            args: "--test sensorium_strip"
          - name: session-roundtrip
            args: "--test session_roundtrip"
          - name: session-route
            args: "--test session_route"
          - name: shipped-binary-version-alignment
            args: "--test shipped_binary_version_alignment"
          - name: spec-audit-c1cb
            args: "--test spec_audit_c1cb"
          - name: spec-audit-multi
            args: "--test spec_audit_multi"
          - name: spore-e2e-fixture
            args: "--test spore_e2e_fixture"
          - name: tackle-adapter-flag
            args: "--test tackle_adapter_flag"
          - name: tackle-claude-config-dir
            args: "--test tackle_claude_config_dir"
          - name: tackle-inprocess-completion
            args: "--test tackle_inprocess_completion"
          - name: tackle-inprocess-no-harvest
            args: "--test tackle_inprocess_no_harvest"
          - name: tag-single-writer
            args: "--test tag_single_writer"
          - name: test-artifact-map
            args: "--test test_artifact_map"
          - name: test-migrate-genre
            args: "--test test_migrate_genre"
          - name: test-migrate-solo-total
            args: "--test test_migrate_solo_total"
          - name: tracing-subscriber-installed
            args: "--test tracing_subscriber_installed"
          - name: verdict-polarity-coherence
            args: "--test verdict_polarity_coherence"
          - name: verify-graph
            args: "--test verify_graph"
          - name: zombie-prevention
            args: "--test zombie_prevention"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
        with:
          shared-key: test-shard
      - name: Run this target
        run: |
          { echo "target=${{ matrix.name }}"; nproc; df -h /; free -m || true; } || true
          cargo test -p cosmon-cli --locked ${{ matrix.args }} -- --nocapture

  # The half nextest cannot see at all: it does not run doctests. 116 of them
  # here, in one opaque `rustdoc` phase, so a doctest that hangs is invisible to
  # every per-test instrument and would have been misread as an interaction
  # between ordinary tests.
  test-doc:
    name: Test (doctests — diagnostic, gates nothing)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    continue-on-error: true
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
        with:
          shared-key: test-shard
      - name: Run the doctests
        timeout-minutes: 30
        continue-on-error: true
        run: |
          set -o pipefail
          { echo "nproc=$(nproc)"; df -h /; free -m || true; } | tee doc.log
          set +e
          timeout --signal=INT --kill-after=30s 25m \
            cargo test --workspace --locked --doc 2>&1 | tee -a doc.log
          rc=${PIPESTATUS[0]}
          set -e
          echo "doc_rc=$rc" | tee -a doc.log
          exit "$rc"
      - name: Upload the doctest output
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: doctests
          path: doc.log
          if-no-files-found: warn

  test:
    name: Test
    runs-on: ubuntu-latest
    # The only job here that carried no bound, while three of its neighbours do.
    # On 2026-07-30 it died twice at ~50 minutes on `cargo test --workspace`,
    # both times with the step's conclusion empty and NO LOG ARCHIVE UPLOADED —
    # the runner was lost rather than a test asserting false, so the failure was
    # undiagnosable from outside. An explicit bound makes Actions cut the job
    # itself, which uploads what ran. This does not fix that failure; it makes
    # the next occurrence readable, which nothing local could.
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      # T-CST-PARITY: the cs ↔ cs-thin parity test in
      # `crates/cosmon-thin-cli/tests/parity_with_cs.rs` exec's the cs
      # binary as a subprocess. `cargo test --workspace` does not build
      # binaries by default, so we explicitly build cs first; the test's
      # `find_cs_binary()` then resolves it from `target/debug/cs`.
      # See docs/guides/cs-thin-parity.md §CI for the rationale.
      - name: Build cs binary (parity gate prerequisite)
        run: cargo build --bin cs -p cosmon-cli --locked
      - run: cargo test --workspace --locked

  # NOTE: the `llama-mock` job was removed in the pre-publication scope trim
  # (delib-20260622-187a B1 / ADR-126). It tested the in-process llama.cpp
  # wrapper (`cargo test -p cosmon-llama --features mock-ffi`), but the
  # `cosmon-llama` / `cosmon-llama-sys` crates and the `mock-ffi` feature on
  # `cosmon-provider` were all git-rm'd — the job could only fail on an
  # unknown package / unknown feature. A Rust-native local-model path will be
  # reconsidered separately (see crates/cosmon-provider/Cargo.toml NOTE).

  cosmon-without-neurion:
    # Mandatory blocking gate — delib-20260418-1f29 outcomes.md Child C.
    # Runs the cosmon substrate tests with PATH stripped of any neurion
    # binary so any drift back toward neurion-coupled discovery breaks
    # the build. Mirrors `just test-without-neurion`. See
    # docs/architectural-invariants.md §7c (Markov property /
    # restart-fidelity) and docs/CONTRIBUTING.md.
    name: cosmon-without-neurion (mandatory)
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Install just
        uses: taiki-e/install-action@e0475f5c1b710607c5f74c0e9f71a9d5f0b62aa5  # just
      - name: Run gate (PATH-stripped)
        run: just test-without-neurion

  spec-conformance:
    # Proptest refuter for docs/specs/CosmonRun.tla — ADR-052 / delib-ffc6
    # Chantier 1. Runs on every PR with PROPTEST_CASES=256 (~20s budget).
    # Nightly deep-suite (50 000 cases) lives in spec-conformance-deep.
    name: Spec conformance (CosmonRun.tla / PR)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Proptest against TLA+ action alphabet
        env:
          PROPTEST_CASES: "256"
          PROPTEST_MAX_SHRINK_ITERS: "4096"
        run: cargo test -p cosmon-core --release --test spec_conformance --locked
      - name: Upload regressions on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: proptest-regressions
          path: crates/cosmon-core/proptest-regressions/
          retention-days: 30

  spec-conformance-deep:
    # Nightly deep-suite — only runs on the cron schedule. Uses
    # PROPTEST_CASES=50000 and `-- --ignored` to pick up the large
    # trace-length test in tests/spec_conformance.rs.
    name: Spec conformance (nightly, 50k cases)
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Deep proptest sweep (nightly)
        env:
          PROPTEST_CASES: "50000"
        run: cargo test -p cosmon-core --release --test spec_conformance --locked -- --ignored
      - name: Upload regressions on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: proptest-regressions-nightly
          path: crates/cosmon-core/proptest-regressions/
          retention-days: 30

  typed-links-conformance:
    # Proptest refuter for MoleculeLink structural invariants — task f214.
    # Runs on every PR with PROPTEST_CASES=256. Covers S1..S5:
    # symmetric Blocks / no cycle / decay monotonicity / entangled symmetry
    # / decay chain finite. See crates/cosmon-core/tests/typed_links_conformance.rs.
    name: Typed links conformance (MoleculeLink / PR)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Proptest over MoleculeLink invariants
        env:
          PROPTEST_CASES: "256"
          PROPTEST_MAX_SHRINK_ITERS: "4096"
        run: cargo test -p cosmon-core --release --test typed_links_conformance --locked
      - name: Upload regressions on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: proptest-regressions-typed-links
          path: crates/cosmon-core/proptest-regressions/
          retention-days: 30

  typed-links-conformance-deep:
    # Nightly deep sweep for the MoleculeLink invariants — 50 000 cases,
    # picks up the `#[ignore]` deep test in typed_links_conformance.rs.
    name: Typed links conformance (nightly, 50k cases)
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Deep proptest sweep (nightly)
        env:
          PROPTEST_CASES: "50000"
        run: cargo test -p cosmon-core --release --test typed_links_conformance --locked -- --ignored
      - name: Upload regressions on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: proptest-regressions-typed-links-nightly
          path: crates/cosmon-core/proptest-regressions/
          retention-days: 30

  doc:
    name: Documentation
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      RUSTDOCFLAGS: -D warnings
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - run: cargo doc --workspace --no-deps --locked

  assert-guard:
    name: Assert Guard
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          fetch-depth: 0
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - name: Check for removed test assertions
        run: |
          BASE="${{ github.event.pull_request.base.sha }}"
          HEAD="${{ github.event.pull_request.head.sha }}"

          # Match removed lines containing Rust test assertions
          PATTERN='^\-.*\b(assert!|assert_eq!|assert_ne!|debug_assert!|debug_assert_eq!|debug_assert_ne!|#\[should_panic)'

          REMOVED=$(git diff "$BASE".."$HEAD" -- '*.rs' | grep -P "$PATTERN" || true)

          if [ -n "$REMOVED" ]; then
            echo "::warning::Test assertions were removed in this PR. Review required."
            echo ""
            echo "The following assertion lines were removed:"
            echo "$REMOVED"
            echo ""
            echo "If these removals are intentional, add 'assert-guard: reviewed' to the PR description."

            # Check if PR description contains the override marker
            PR_BODY="${{ github.event.pull_request.body }}"
            if echo "$PR_BODY" | grep -q 'assert-guard: reviewed'; then
              echo ""
              echo "Override marker found in PR description. Proceeding."
              exit 0
            fi

            exit 1
          fi

          echo "No test assertions removed. ✓"

  spawn-matrix:
    name: Spawn matrix (fake tmux + claude)
    # NON-BLOCKING CANARY: the fake-tmux shim harness has never held on
    # headless runners (cs env-sanitized spawn resolves the real tmux, so
    # the S-bit observation misses). Tracked for a structural fix in the
    # spawn-matrix molecule; keep the signal visible without gating merges.
    continue-on-error: true
    if: ${{ github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Build cs binary
        run: cargo build --bin cs -p cosmon-cli --locked
      - name: Run spawn matrix
        env:
          MATRIX_OUTDIR: matrix-artifacts
        run: bash tests/harness/run_matrix.sh
      - name: Upload matrix artifacts on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: spawn-matrix-artifacts
          path: matrix-artifacts/
          retention-days: 7

  cross-adapter-smoke:
    # Tier-2 cross-Adapter integration tests (ADR-098 / C8). Requires
    # the real `claude` and `aider` binaries on PATH plus tmux; runs
    # only on the nightly cron schedule and is non-blocking
    # (continue-on-error: true). The 90-day forensic gate (C9) consumes
    # the resulting events.jsonl, not the per-run pass/fail.
    name: Cross-Adapter smoke (requires aider + claude binaries)
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    continue-on-error: true
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Tier-2 smoke (real binaries; #[ignore]-gated)
        run: cargo test -p cosmon-transport --locked -- --ignored cross_adapter

  restart-fidelity-without-neurion:
    # Mandatory gate against Universe D paradigm drift
    # (delib-20260418-8166). Runs the integration test that exercises a
    # multi-molecule DAG trajectory under a sandboxed HOME with no
    # neurion service, no LaunchAgent, and no external scheduler — the
    # operative version of the Markov property
    # (docs/architectural-invariants.md §7c).
    #
    # If this job fails, do NOT merge to bypass it. Either fix the
    # regression or raise a successor ADR explaining why cosmon should
    # no longer be required to run standalone.
    name: Restart fidelity without neurion
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Run the standalone restart-fidelity gate
        # Strip every NEURION_* env that may leak from the runner image
        # so the gate cannot pass for the wrong reason. The test re-
        # exports a sandboxed NEURION_AUTO_REGISTER_FILE per-invocation.
        env:
          NEURION_SOCKET: ""
          NEURION_DB_PATH: ""
        run: cargo test -p cosmon-cli --test restart_fidelity_no_neurion --locked

  smoke-lifecycle:
    name: Lifecycle smoke (nucleate → done)
    # NON-BLOCKING CANARY: the fake-tmux shim harness has never held on
    # headless runners (cs env-sanitized spawn resolves the real tmux, so
    # session-state observations miss). Tracked for a structural fix in the
    # spawn-matrix molecule; keep the signal visible without gating merges.
    continue-on-error: true
    if: ${{ github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: Build cs binary
        run: cargo build --bin cs -p cosmon-cli --locked
      - name: Run full-lifecycle smoke suite
        env:
          SMOKE_OUTDIR: smoke-artifacts
        run: bash tests/harness/smoke/run_all.sh
      - name: Upload smoke artifacts on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: smoke-lifecycle-artifacts
          path: |
            target/smoke-*/
            smoke-artifacts/
          retention-days: 7

  provenance-gate:
    name: Provenance gate (ADR-052 §I9)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          fetch-depth: 0
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      # Names the refusal ADR-052 §D5 actually assigns to CI: the
      # `(<mol_id>)` provenance line on every merge commit. The ledger half
      # of §I9 belongs to .cosmon/hooks/pre-merge-commit, which reads a
      # working tree; on this repo's ADR-055 team residence `.cosmon/state/`
      # is gitignored, so no tree here can carry the ledger and the script
      # says so up front. See task-20260729-dc53.
      - name: Validate merge-commit provenance (ledger where residence keeps it)
        run: bash scripts/check-provenance.sh
      - name: Self-test the gate against the c1cb pathology
        run: bash tests/harness/provenance-gate-test.sh
      - name: Self-test the base-sync exemption (ADR-052 §D5-bis)
        run: bash tests/harness/provenance-base-sync-test.sh
      - name: Self-test ledger coverage vs residence (ADR-055)
        run: bash tests/harness/provenance-residence-test.sh

  license-partition:
    # Living per-crate license table gate. The script in
    # scripts/license-table.sh walks every crates/*/Cargo.toml,
    # resolves the effective license, and:
    #   - fails if a Cargo.toml declares anything other than
    #     AGPL-3.0-only or Apache-2.0 (someone invented a third licence);
    #   - fails if a crate has no row in scripts/license-rationales.tsv
    #     (a new crate was added without applying the ADR-092 §3
    #     placement test);
    #   - fails if docs/license/INDEX.md is stale relative to the
    #     workspace + mapping (drift between code and doc).
    # Operator fix: bash scripts/license-table.sh --write.
    # See ADR-092 + docs/license/INDEX.md.
    name: License partition consistent
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Install system libdbus (Linux keyring backend)
        if: runner.os == 'Linux'
        run: sudo apt-get update -q && sudo apt-get install -y -q libdbus-1-dev pkg-config
      - name: Per-crate license partition (script + doc + Cargo.toml)
        run: bash scripts/license-table.sh --check

  # NOTE: the `matrix-bridge-isolation` job was removed in the pre-publication
  # scope trim (delib-20260622-187a B1 / ADR-126). It guarded the read-only
  # ingress discipline of `crates/cosmon-matrix-tick`, which was git-rm'd; the
  # forbid-matrix-*.sh scripts only no-op'd over an absent crate. Restore the
  # job together with the crate if the Matrix bridge is ever re-introduced.

  # NOTE: README quickstart verification lives in its own workflow at
  # .github/workflows/readme-quickstart.yml — it runs on both Ubuntu and
  # macOS and executes the full bootstrap end-to-end.
  #
  # NOTE: cargo-deny + cargo-audit have been moved to
  # .github/workflows/deny.yml (supply-chain gate, runs on push/PR/schedule).
