\* Model 2 — Convoy-cascade counterexample (2026-04-12 chronicle).
\* S3 is disabled: `patrol-propel` may fire regardless of sediment.
\* Autopilot guard is also disabled so nothing else prevents the
\* cascade.
\* Expected: TLC reports a violation of S3_PurgeBeforeRespawn with a
\* short trace: Init -> ActivityAccrues -> Fire(patrol-propel). The
\* firing of patrol-propel on non-empty sediment flips
\* cascade_detected = TRUE, which is the mechanical restatement of
\* "the runtime resurrected a zombie subgraph".
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTRAINT StateBound
CONSTANTS
    MaxTime          = 2
    MaxSediment      = 1
    S3Enabled        = FALSE
    AutopilotEnabled = FALSE
    BacklogThreshold = 2
INVARIANTS
    S3_PurgeBeforeRespawn
