\* Model 3 — Async crashes enabled, BypassMerge disabled.
CHECK_DEADLOCK FALSE
\* Expected: I3 and I4 FAIL as stepwise safety (TmuxCrash and
\* ProcessCrash break them transiently); I6, I7, I9 still hold.
\* Liveness L2 still holds because Purge has weak fairness.
\* This run documents the safety-vs-eventual-consistency frontier:
\* I3 / I4 must be reformulated as liveness once the environment
\* is modeled honestly.
SPECIFICATION Spec
CONSTANTS
    \* MaxSeqno dropped from 2 to 1 on 2026-04-20: adding sealLog/now
    \* to the state vector (I_StepProgress amendment) expanded the
    \* Crashes.cfg state space beyond the practical tla-verify budget
    \* (3 molecules × MaxSeqno=2 × sealLog-as-Seq(Nat) ≈ 10M+ states,
    \* > 10 min on Apple M-series). MaxSeqno=1 still exercises the
    \* writer-lock single-owner discipline (I7) and the async-crash
    \* safety-vs-eventual-consistency frontier (I3/I4) which are what
    \* this config is meant to check. The stricter safety variants
    \* CrashesI3 and CrashesI4 already use MaxSeqno=1.
    Mol = {m1, m2, m3}
    MaxSeqno = 1
    AsyncCrashesEnabled = TRUE
    OutOfBandEnabled    = FALSE
    T_STALL  = 99
    MaxClock = 1
INVARIANTS
    TypeOK
    I6_NoGhostFleetEntry
    I7_SingleEventWriter
    I9_BranchMergedOnlyIfCompleted
PROPERTIES
    L2_DeadSessionEventuallyPurged
    L3_LockEventuallyReleased
